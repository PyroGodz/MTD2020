function myuf(a)
{
var t  = 0o33;
if(t<0 ) 
{alert('because of minus t becomes to zero');
t = 0;}
if(!Number.isInteger(t)){alert('not an Integer, t will be rouned'); t=Math.round(j);}

t  = t  + a ;
if(t<0 ) 
{alert('because of minus t becomes to zero');
t = 0;}
if(!Number.isInteger(t)){alert('not an Integer, t will be rouned'); t=Math.round(j);}

return t ;
}


{
var check  = true;
document.write('<br>');
document.write(check);
var str  = "hello";
var sss  = str.slice(0);
document.write('<br>');
document.write(sss);
var m  = myuf(3);
if(m<0 ) 
{alert('because of minus m becomes to zero');
m = 0;}
if(!Number.isInteger(m)){alert('not an Integer, m will be rouned'); m=Math.round(j);}

document.write('<br>');
document.write(m);
var inverse  =  ~ 15;
if(inverse<0 ) 
{alert('because of minus inverse becomes to zero');
inverse = 0;}
if(!Number.isInteger(inverse)){alert('not an Integer, inverse will be rouned'); inverse=Math.round(j);}

document.write('<br>');
document.write(inverse);
var or  = 55 | 80;
if(or<0 ) 
{alert('because of minus or becomes to zero');
or = 0;}
if(!Number.isInteger(or)){alert('not an Integer, or will be rouned'); or=Math.round(j);}

var and  = 56 & 76;
if(and<0 ) 
{alert('because of minus and becomes to zero');
and = 0;}
if(!Number.isInteger(and)){alert('not an Integer, and will be rouned'); and=Math.round(j);}

if (or  > and)
{
document.write('<br>');
document.write(or);
}
else
{
document.write('<br>');
document.write(and);
}
var n  = "привет";
var s  = "котята";
var ns  = n + s;
document.write('<br>');
document.write(ns);
var j  = ((44 + 4) / 7);
if(j<0 ) 
{alert('because of minus j becomes to zero');
j = 0;}
if(!Number.isInteger(j)){alert('not an Integer, j will be rouned'); j=Math.round(j);}

document.write('<br>');
document.write(j);
}
